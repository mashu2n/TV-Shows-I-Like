import React from "react";

import { Button } from "react-bootstrap";
import "../Styles/Button.css";

const SubmitButton = (props) => {
  return <Button className="button">Comment</Button>;
};

export default SubmitButton;
